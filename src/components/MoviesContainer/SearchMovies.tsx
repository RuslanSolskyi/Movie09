import React from 'react';
import MoviesList from "./MoviesList";

const SearchMovies = () => {
    return (
        <div>

            <MoviesList/>
        </div>
    );
};

export default SearchMovies;